.container
  %table
    %thead
      %th Name 
      %th Email
      %th Birthdate
      %th
    %tbody  
    - @users.each do |user| 
      %tr
        %td= user.first_name
        %td= user.email
        %td= user.birthdate
        %td
          = link_to 'Edit', edit_dashboard_user_path(user)
          |
          = link_to 'Delete', dashboard_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }